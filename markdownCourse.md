# Advanced Probabilities

Vincent Marc LAMBERT, ESILV

7 juin 2025
# Table of Contents

1 Sigma-Algebra
2 Probability Measure
3 Conditional Probability
4 A bit of measure theory
5 Independence
6 Gaussian Vectors
7 Conditional Expectation
8 Change of Law
9 Random Process, Martingale and Stopping Time
10 Poisson Process
# Introduction

- Louis Bachelier (1870-1946) :

- 1895 : licence es Sciences.
- 1900 : thesis in Paris under the supervision of Henri Poincaré "Théorie de la spéculation" where he used Brownian motion to model the dynamics of asset prices.
- First mathematical theory of the Brownian motion (5 years before Einstein).

- Einstein (1879-1955) :

- Draw the attention of the physicist community on Brownian motion.
- Predicted that it might not be possible to prove that the Brownian motion trajectory is continuous.

- Norbert Wiener (1894-1964) :

- Proved continuity of the trajectory during the 1920s.
# Bibliography

Below is a complementary (and not exhaustive) bibliography :

- M. Cottrell, V. Genon-Catalot, C. Duhamel and T. Meyre. Exercices de Probabilités. Cassini
- D. Foata, J. Franchi, A. Fuchs. Calcul des probabilités, 3rd Edition, Dunod.
- J. Jacod, P. Protter. L'essentiel en théorie des probabilités, Cassini.
- B. Jourdain. Probabilités et Statistique, Ellipses.
- S. Méléard. Aléatoire, Ecole Polytechnique.
- T. Meyre. Probabilités, cours et exercices corrigés, Tome 1, Calvage & Mounet.
- D. Revuz. Probabilités, Hermann.
# Objectives

- Providing some recapitulation of useful probabilistic tools which are prerequisites to any modelisation of random events with tools as elasticsearch, kibana,... or in quantum computing.
- In order to do quantitative economics/insurance/finance, one needs to model uncertainty as for example with Bloomberg or SAS.

![img-0.jpeg](img-0.jpeg)

FIGURE – bloomberg
# Sigma-Algebra :Definition 1

$$\mathcal{T} \subset \mathcal{P}(\Omega)$$ is a $$\sigma$$-algebra on $$\Omega$$ if

1. $$\emptyset \in \mathcal{T}$$
2. $$A \in \mathcal{T} \Rightarrow A^c := \{x \in \Omega, x \notin A\} \in \mathcal{T}$$
3. $$(A_n)_{n \in \mathbb{N}} \subset \mathcal{T} \Rightarrow \bigcup_{n \in \mathbb{N}} A_n := \{x \in \Omega, \exists n \in \mathbb{N}, x \in A_n\} \in \mathcal{T}$$

The elements of $$\mathcal{T}$$ are called the events or outcomes.
# Examples

1 Trivial sigma-algebra $\{\emptyset, \Omega\}$.
2 Sigma-algebra of all events : the power set $\mathcal{P}(\Omega) = \{A, A \subset \Omega\}$.
3 Sigma-algebra associated to a partition $(A_i)_{i \in I}$ i.e. $\Omega = \bigcup_{i \in I} A_i : \left\{ \bigcup_{j \in J} A_j \right\}_{J \subset \mathcal{P}(I)}$.
# Definition 2

Let $\mathcal{T}_t \subset \mathcal{P}(\Omega)$ be a $\sigma$-algebra on $\Omega$ for $t \geq 0$. Then $(\mathcal{T}_t)_{t > 0}$ is a filtration if

1. $\mathcal{T}_s \subset \mathcal{T}_t$ as soon as $s \leq t$.
2. $\mathcal{T}_t$ is the information available at time $t$.
# Definition 3

For any $\mathcal{C} \subset \mathcal{P}(\Omega)$, there exists a smallest sigma-algebra on $\Omega$—in the sense of the inclusion—which contains $\mathcal{C}$, it is called the sigma-algebra generated by $\mathcal{C}$ and denoted by $\sigma(\mathcal{C})$.
# Definition 4

Let $\mathcal{T}$ be a sigma-algebra on $\Omega$. A (real-valued) random variable $X$ is a function from $\Omega$ to $\mathbb{R}$, such that for every $B \in \mathcal{B}(\mathbb{R})$,

$\{X \in B\} := X^{-1}(B) = \{\omega \in \Omega, X(\omega) \in B\} \in \mathcal{T}$ i.e. $\{X \in B\}$ is an event.
# Definition 5

Let $$(\mathcal{T}_t)_{t\geq 0}$$ be a filtration on $$\Omega$$. A stochastic process $$(X_t)_{t\geq 0}$$ is a sequence of random variables from $$\Omega$$ to $$\mathbb{R}$$, such that for every $$B\in \mathcal{B}(\mathbb{R})$$, $$\{X_t\in B\} := X_t^{-1}(B)\in \mathcal{T}_t$$ for all $$t > 0$$.
# Probability Measure :Definition 6

Let $(\Omega, \mathcal{T})$ be a sample space and a sigma-algebra. A function $\mathbb{P}$ from $\mathcal{T}$ to $\mathbb{R}$ is called a probability measure if

1. $\mathbb{P}(\Omega) = 1$
2. $\mathbb{P}(A) \geq 0$, for all events $A \in \mathcal{T}$
3. If $(A_n)_{n \in \mathbb{N}}$ is a sequence of mutually exclusive events i.e. $i \neq j \Rightarrow A_i \cap A_j = \emptyset$ then $\mathbb{P}\left(\bigcup_{n \in \mathbb{N}} A_n\right) = \sum_{n \in \mathbb{N}} \mathbb{P}(A_n)$
# Conditional Probability :Definition 7

In the probability space $$(\Omega, \mathcal{T}, \mathbb{P})$$, the conditional probability of the event $$A$$ knowing an event $$B$$ such that $$\mathbb{P}(B) > 0$$ is denoted by $$\mathbb{P}(A \mid B)$$ and defined by

$$\mathbb{P}(A \mid B) = \frac{\mathbb{P}(A \cap B)}{\mathbb{P}(B)}$$
# A bit of measure theory :Theorem 1

The Lebesgue measure is the unique measure on $$(\mathbb{R}, \mathcal{B}(\mathbb{R}))$$ that is

1 stable by translation and such that
2 $$\lambda([0, 1]) = 1$$.

The Lebesgue measure of an interval is just its length. The Lebesgue measure $$\lambda_d$$ on $$(\mathbb{R}^d, \mathcal{B}(\mathbb{R}^d))$$ is the unique measure that associates to hypercubes their volume. The Lebesgue measure $$\lambda_d$$ on $$(\mathbb{R}^d, \mathcal{B}(\mathbb{R}^d))$$ is the product measure $$\lambda^{\otimes d}$$ defined on $$\mathcal{B}(\mathbb{R}^d)$$.
# Definition 10

The counting measure on $(E, \mathcal{E})$ is defined by $\mu(A) = \text{card}(A)$ if $\text{card}(A) < \infty$ and $\infty$ else.

The Lebesgue measure of any set which is finite or countable is equal to zero.

$$\lambda(\{a\}) = \lambda \left( \bigcap_{n \ge 0} \left[ a - \frac{1}{n}, a + \frac{1}{n} \right] \right) = \lim_{n \to \infty} \downarrow \lambda \left( \left[ a - \frac{1}{n}, a + \frac{1}{n} \right] \right) \tag{1}$$

$$= \lim_{n \to \infty} \downarrow \frac{2}{n} = 0 \tag{2}$$
# Definition 10

Vocabulary :

- $A$ is $\mu$-negligible if there exist $B \in \mathcal{T}$ such that $A \subset B$ and $\mu(B) = 0$.
- A property $\mathcal{P}$ which depends on $\omega$ is true $\mu$-almost everywhere or almost surely (a.e. or a.s.) if $\{\omega \in \Omega \mid \mathcal{P}(\omega) \text{ does not hold true}\}$ is $\mu$-negligible.

Examples : Any set which is finite or countable is negligible for the Lebesgue measure. Only the emptyset is negligible for the counting measure.
# Objective

Integrate a function $f$ with respect to a measure $\mu$ which will be the Lebesgue or the counting measure.

- Case 1 : $\mu$ is the counting measure. In good cases

$$\int_E f \, d\mu = \sum_{x \in E} f(x)$$

- Case 2 : $\mu$ is the Lebesgue measure. $\int_E f \, d\mu$ is the Lebesgue integral. In good cases, it can be computed thanks to the Riemann integral. It is important to review the well-known primitives.
- Case 3 : $\mu$ has a density with respect to the Lebesgue or the counting measure, see below for the computation of $\int_E f \, d\mu$.
# Lemma 1

Let $(E, \mathcal{E}, \mu)$ be a measured space with $E = \mathbb{R}, \mathbb{R}^d, \mathbb{N}, \mathbb{N}^d$, $\mathcal{E}$ a sigma-algebra on $E$, and $\mu$ the Lebesgue or counting measure on $\mathcal{E}$. Let $f$ be a non-negative function in $\mathcal{L}_{\mathbb{R}^+}^1(E, \mathcal{E}, \mu)$.

Let $\nu : \mathcal{E} \to \mathbb{R}$ be defined by

$$\nu(B) := \int_B f \, d\mu, \quad \forall B \in \mathcal{E}$$

$\nu$ is a measure on $(E, \mathcal{E})$ and $f$ is called a density function on $E$. $\nu$ is a measure with density $f$ with respect to $\mu$. Notation :

$$\nu = f \cdot \mu.$$
# Lemma 1

If $\int_E f \, d\mu = 1$, then $f$ is a density of probability on $E$ and $\nu$ is a probability measure with density $f$ with respect to $\mu$.

A random variable $X : (\Omega, \mathcal{T}, \mathbb{P}) \to (E, \mathcal{E}, \mu)$ has density $f_X$ with respect to $\mu$ if $\mathbb{P}_X$ has density $f_X$ with respect to $\mu$, i.e.,

$$\mathbb{P}_X = f_X \cdot \mu.$$
# Theorem 2 : Transfer's Theorem

Let $\mathbb{K}$ and $\mathbb{K}' = \mathbb{R}, \mathbb{R}^d$ or $\mathbb{N}, \mathbb{N}^d$. Let $X \in \mathcal{L}_{\mathbb{K}}^1(\Omega, \mathcal{T}, \mu)$ and a measurable function $g$ from $(\mathbb{K}, \mathcal{B}(\mathbb{K}))$ to $(\mathbb{K}', \mathcal{B}(\mathbb{K}'))$.

Then, if everything is well-defined,

$$\mathbb{E}(g(X)) = \int_{\Omega} g(X(\omega)) \mathbb{P}(d\omega) = \int_{\mathbb{K}} g(x) \mathbb{P}_X(dx)$$

If $g \geq 0$, the above equation is always true.

If $\mathbb{K} = \mathbb{K}' = \mathbb{R}$ and $g = \text{id}$, $\mathbb{E}(X) = \int_{\mathbb{K}} x \mathbb{P}_X(dx)$.
# Example from Finance

Assume that the price $S_T$ at time $T$ of the stock follows a log-normal law, i.e., $\ln \left( \frac{S_T}{s_0} \right) \sim \mathcal{N}(r, \sigma^2 T)$ under the so-called risk-neutral measure. We want to compute the price of a call option which pays at $T$ the payoff $H = (S_T - K)_+$, where $K$ is the strike price. In a good market, this price is given by

$$\mathbb{E} \left( (S_T - K)_+ \right) = \int_{\Omega} (S_T(\omega) - K)_+ \mathbb{P}(d\omega)$$

$$\mathbb{E} \left( (S_T - K)_+ \right) = \int_{\Omega} \left( s_0 e^{\ln \left( \frac{S_T(\omega)}{s_0} \right)} - K \right)_+ \mathbb{P}(d\omega)$$
# Example from Finance

$$\mathbb{E}\left((S_{T}-K)_{+}\right)=\int_{\mathbb{R}}\left(s_{0}e^{x}-K\right)_{+}\mathbb{P}_{\ln\left(\frac{s_{T}}{s_{0}}\right)}(dx)$$

$$\mathbb{E}\left((S_{T}-K)_{+}\right)=\int_{\mathbb{R}}\left(s_{0}e^{x}-K\right)_{+}\frac{1}{\sqrt{2\pi\sigma^{2}T}}e^{-\frac{(x-r)^{2}}{2\sigma^{2}T}}dx$$
# Independence :Definition 22

Let $$(\Omega, \mathcal{T}, \mathbb{P})$$ be a probability space. The events $$A$$ and $$B$$ of $$\mathcal{T}$$ are independent if

$$\mathbb{P}(A \cap B) = \mathbb{P}(A)\mathbb{P}(B)$$

- $$\mathbb{P}(A) = 0$$ or $$\mathbb{P}(B) = 0 \Rightarrow A$$ and $$B$$ are independent.
- If $$\mathbb{P}(A) = 0$$, as $$A \cap B \subset A$$, $$0 \leq \mathbb{P}(A \cap B) \leq \mathbb{P}(A) = 0$$.

The notion of independence depends on the chosen probability measure.
# Example of the stock price

Assume again that for 0 < p < 1

$$\mathbb{P}(\{UU\}) = p^2, \quad \mathbb{P}(\{UD\}) = \mathbb{P}(\{DU\}) = p(1-p), \quad \mathbb{P}(\{DD\}) = (1-p)^2$$

When are $\{UU, UD\} = A$ and $\{DU, UD\} = B$ independent?

As $p > 0$ and $p < 1$, $A$ and $B$ are independent if and only if

$$\mathbb{P}(A \cap B) = \mathbb{P}(\{UD\}) = \mathbb{P}(A)\mathbb{P}(B)$$

if and only if $p(1-p) = (p^2 + p(1-p)) \cdot 2p(1-p)$

if and only if $p(1-p) = 2p^2(1-p)$

if and only if $p = \frac{1}{2}$.
# Definition 23

Let $$(\Omega, \mathcal{T}, \mathbb{P})$$ be a probability space and $$I$$ some non-empty set. The events $$(A_i)_{i \in I}$$ of $$\mathcal{T}$$ are independent (mutually independent) if

- For all $$k \geq 1$$, $$i_1 < \ldots < i_k$$ in $$I$$
- Thus for all finite sub-families

$$\mathbb{P}(A_{i_1} \cap \ldots \cap A_{i_k}) = \mathbb{P}(A_{i_1}) \ldots \mathbb{P}(A_{i_k})$$

Events can be independent two by two (pairwise independent) but not independent.
# Definition 23

Let $\Omega = \{\omega_1, \ldots, \omega_4\}$, $\mathcal{T} = \mathcal{P}(\Omega)$:

- $\mathbb{P}(\{\omega_i\}) = \frac{1}{4}$ for $i \in \{1, \ldots, 4\}$.
- Let $A_1 = \{\omega_1, \omega_4\}$, $A_2 = \{\omega_2, \omega_4\}$, and $A_3 = \{\omega_3, \omega_4\}$.

$A_1$ and $A_2$ are independent, $A_2$ and $A_3$ are independent, $A_1$ and $A_3$ are independent since

$$\mathbb{P}(A_1 \cap A_2) = \mathbb{P}(A_1 \cap A_3) = \mathbb{P}(A_2 \cap A_3) = \mathbb{P}(\{\omega_4\}) = \frac{1}{4}$$

$$\mathbb{P}(A_i)\mathbb{P}(A_j) = \left(\frac{1}{4} + \frac{1}{4}\right) \left(\frac{1}{4} + \frac{1}{4}\right) = \frac{1}{4}$$
# Definition 23

But $A_1, A_2, A_3$ are not independent since

$$\mathbb{P}(A_1 \cap A_2 \cap A_3) = \frac{1}{4} \quad \text{and} \quad \mathbb{P}(A_1)\mathbb{P}(A_2)\mathbb{P}(A_3) = \frac{1}{8}$$
# Superior and Inferior Limits

- Superior limit of $(A_n)_{n\in\mathbb{N}}$, $\limsup_n A_n = \bigcap_{k\in\mathbb{N}} \bigcup_{n\ge k} A_n$.
- Interpretation : "An infinity of events $A_n$ are realized", whatever the rank $k$, there exists events after $k$ that are realized.
- Inferior limit of $(A_n)_{n\in\mathbb{N}}$, $\liminf_n A_n = \bigcup_{k\in\mathbb{N}} \bigcap_{n\ge k} A_n$.
- Interpretation for $\liminf_n A_n^c$ : "A finite number of events $A_n$ are realized", after a given rank $k$, all the realized events are the complementary of $A_n$.
- $(A_n)_{n\in\mathbb{N}} \subset \mathcal{T} \Rightarrow \liminf_n A_n, \limsup_n A_n \in \mathcal{T}$.
# Borel-Cantelli Lemma

Let $(A_n)_{n\in\mathbb{N}}$ be a sequence of events in $(\Omega, \mathcal{T}, \mathbb{P})$ then

$$\sum_{n=0}^{\infty} \mathbb{P}(A_n) < \infty \Rightarrow \mathbb{P}\left(\limsup_n A_n\right) = 0 \tag{3}$$

If $(A_n)_{n\in\mathbb{N}}$ are independent, then

$$\sum_{n=0}^{\infty} \mathbb{P}(A_n) = +\infty \Leftrightarrow \mathbb{P}\left(\limsup_n A_n\right) = 1 \tag{4}$$
# Borel-Cantelli Lemma

- Law of 0 or 1, the probability measure of $\limsup_n A_n$ can only be 0 or 1 depending on the convergence of the series $(\mathbb{P}(A_n))_{n\in\mathbb{N}}$.
- First part does not need independence assumption.
# Definition 25

Let $$(\Omega, \mathcal{T}, \mathbb{P})$$ be a probability space and some non-empty set $$I$$. Let $$(\mathcal{T}_i)_{i \in I}$$ be a sequence of sigma-algebras such that $$\mathcal{T}_i \subset \mathcal{T}$$. The $$\mathcal{T}_i$$ ($$i \in I$$) are independent if and only if

$$\forall (A_1, \dots, A_N) \in \mathcal{T}_1 \times \dots \times \mathcal{T}_N$$

$$\mathbb{P}(A_1 \cap \dots \cap A_N) = \mathbb{P}(A_1) \dots \mathbb{P}(A_N)$$
# Definition 26

Let $$(\Omega, \mathcal{T}, \mathbb{P})$$ be a probability space and some non-empty set $$I$$. The random variables $$(X_i)_{i \in I}$$ from $$(\Omega, \mathcal{T})$$ to $$(E_i, \mathcal{E}_i)$$ are independent if and only if the sigma-algebras $$(\sigma(X_i))_{i \in I}$$ are independent.

$$\forall A_i \in \mathcal{E}_i, i = 1 \dots N, \quad \mathbb{P}(\{X_1 \in A_1\} \cap \dots \cap \{X_N \in A_N\})$$
$$= \mathbb{P}(\{X_1 \in A_1\}) \dots \mathbb{P}(\{X_N \in A_N\})$$
# Lemma 3

Let $X_1, \ldots, X_n$ be some random variables defined on $(\Omega, \mathcal{T}, \mathbb{P})$ with values in $(\mathbb{R}, \mathcal{B}(\mathbb{R}))$, for all $i = 1, \ldots, n$. The $(X_i)_{1 \leq i \leq n}$ are independent if and only if for all $(x_1, \ldots, x_n) \in \mathbb{R}^n$

$$\mathbb{P}\left(\{X_1 \leq x_1\} \cap \ldots \cap \{X_n \leq x_n\}\right) = \prod_{i=1}^n \mathbb{P}\left(\{X_i \leq x_i\}\right)$$
# Lemma 4

Let $(\Omega, \mathcal{T}, \mathbb{P})$ be a probability space.

Let $I = \{1, \ldots, n\}$ with $n \geq 2$ or $I = \mathbb{N}$.

Assume that the $(X_i)_{i \in I}$ from $(\Omega, \mathcal{T})$ to $(E_i, \mathcal{E}_i)$ are independent.

Let $f_i : (E_i, \mathcal{E}_i) \to (F_i, \mathcal{F}_i)$ be measurable functions.

Then the $(f_i(X_i))_{i \in I}$ are independent random variables.
# Theorem 3

Let $(\Omega_i, \mathcal{T}_i, \mathbb{P}_i)$ be $n$ probability spaces.

- Product sigma-algebra : $\mathcal{T}_1 \otimes \ldots \otimes \mathcal{T}_n$.
- It is the smallest sigma-algebra that contains $\mathcal{T}_1 \times \ldots \times \mathcal{T}_n$.
- Note that $\mathcal{B}(\mathbb{R}) \otimes \ldots \otimes \mathcal{B}(\mathbb{R}) = \mathcal{B}(\mathbb{R}^n)$.

There exists a unique probability measure on $\mathcal{T}_1 \otimes \ldots \otimes \mathcal{T}_n$, denoted by $\mathbb{P}_1 \otimes \ldots \otimes \mathbb{P}_n$ such that for all $A_i \in \mathcal{T}_i$

$$\mathbb{P}_1 \otimes \ldots \otimes \mathbb{P}_n (A_1 \times \ldots \times A_n) = \prod_{i=1}^n \mathbb{P}_i (A_i)$$
# Lemma 5

Let $$(\Omega, \mathcal{T}, \mathbb{P})$$ be a probability space.

Let $$N = n$$ for $$I = \{1, \ldots, n\}$$ with $$n \geq 2$$ or $$N \geq 2$$ if $$I = \mathbb{N}$$.

The random variables $$(X_i)_{i \in I}$$ from $$(\Omega, \mathcal{T})$$ to $$(E_i, \mathcal{E}_i)$$ are independent if and only if

$$\mathbb{P}_{(X_1, \ldots, X_N)} = \mathbb{P}_{X_1} \otimes \ldots \otimes \mathbb{P}_{X_N}$$

$$(X_i)_{i \in I}$$ are independent if and only if $$A_i \in \mathcal{E}_i$$ :

$$\mathbb{P}(\{X_1 \in A_1\} \cap \ldots \cap \{X_N \in A_N\}) = \mathbb{P}(\{X_1 \in A_1\}) \ldots \mathbb{P}(\{X_N \in A_N\})$$
# Lemma 5

$$\mathbb{P}\left(\{X_{1}\in A_{1}\}\cap\ldots\cap\{X_{N}\in A_{N}\}\right)=\mathbb{P}_{(X_{1},\ldots,X_{N})}\left(A_{1}\times\ldots\times A_{N}\right)$$

$$\mathbb{P}_{X_{1}}\otimes\ldots\otimes\mathbb{P}_{X_{N}}\left(A_{1}\times\ldots\times A_{N}\right)=\prod_{i=1}^{N}\mathbb{P}_{X_{i}}\left(A_{i}\right)$$
# Theorem 4

Let $X_1, \ldots, X_n$ be random variables $X_i : (\Omega, \mathcal{T}, \mathbb{P}) \to (E_i, \mathcal{E}_i)$. The following assertions are equivalent :

- $X_1, \ldots, X_n$ are independent.
- For all functions $h_i : E_i \to \mathbb{R}$, $\mathcal{E}_i - \mathcal{B}(\mathbb{R})$ measurable and non-negative

$$\mathbb{E} \left( \prod_{i=1}^n h_i(X_i) \right) = \prod_{i=1}^n \mathbb{E}(h_i(X_i))$$
# Theorem 4

- For all functions $h_i \in \mathcal{L}_{\mathbb{R}}^1(E_i, \mathcal{E}_i, \mathbb{P}_{X_i})$

$$\mathbb{E} \left( \prod_{i=1}^n h_i(X_i) \right) = \prod_{i=1}^n \mathbb{E}(h_i(X_i))$$

If the $h_i$ are bounded then the integrability condition of 3. is satisfied.
# Lemma 6

Let $X_1, \ldots, X_n \in \mathcal{L}_{\mathbb{R}}^1(\Omega, \mathcal{T}, \mathbb{P})$ be independent random variables then

$$\mathbb{E}(X_1 \ldots X_n) = \prod_{i=1}^n \mathbb{E}(X_i)$$
# Theorem 5

Let $X_1, \ldots, X_n : (\Omega, \mathcal{T}, \mathbb{P}) \to (E_i, \mathcal{E}_i, \mathbb{Q}_i)$ be random variables.

Let $\mathbb{Q} = \mathbb{Q}_1 \otimes \ldots \otimes \mathbb{Q}_n$.

Assume that $X_1, \ldots, X_n$ are independent.

Assume that $\mathbb{P}_{X_i} = f_{X_i} \cdot \mathbb{Q}_i$, for $i = 1, \ldots, n$.

Then the density of $(X_1, \ldots, X_n)$ with respect to $\mathbb{Q}$ is

$$(x_1, \ldots, x_n) \to \prod_{i=1}^n f_{X_i}(x_i).$$
# Theorem 5

Conversely, if the density of $(X_1, \ldots, X_n)$ with respect to $\mathbb{Q}$ is $(x_1, \ldots, x_n) \to \prod_{i=1}^n f_i(x_i)$ (with $f_i \in \mathcal{L}_{\mathbb{R}^+}^1(E_i, \mathcal{E}_i, \mathbb{Q}_i)$ for $i = 1, \ldots, n$).

Then $\mathbb{P}_{X_i} = c_i f_i \cdot \mu_i$ and the $X_1, \ldots, X_n$ are independent.
# Gaussian Vectors :Definition 35

A probability measure $\mu$ defined on $\mathcal{B}(\mathbb{R})$ is Gaussian if :

1 Non-degenerated case : if it has a density with respect to the Lebesgue measure : $\mu = f_{m,\sigma^2} \cdot \lambda$ where

$$f_{m,\sigma^2}(y) = \frac{1}{\sqrt{2\pi\sigma^2}} e^{-\frac{(y-m)^2}{2\sigma^2}}$$

where $m \in \mathbb{R}$ and $\sigma^2 \in (0, \infty)$.

2 Degenerated case : if $\mu = \delta_m$ the Dirac mass in $m$, which is defined by $\delta_m(A) = 1$ if $m \in A$ and 0 otherwise.
# Gaussian Vectors :Definition 35

If the law of $X$ is $\mu$, i.e., $\mathbb{P}_X = \mu$, then $\mathbb{E}(X) = \int x \, d\mu = m$, $\mathbb{V}(X) = \sigma^2$. The characteristic function of $\mathcal{N}(m, \sigma^2)$ is :

$$\Phi_X(t) = e^{itm - \frac{t^2 \sigma^2}{2}}$$

Let $(Y_n)_{n \in \mathbb{N}^*}$ and $Y$ be some real-valued random variables.
# Definition 36

$(Y_n)_{n\in\mathbb{N}^*}$ converges in law to $Y$, denoted $Y_n \xrightarrow[n\to+\infty]{\text{law}} Y$, if and only if for all bounded and continuous functions $f : \mathbb{R} \to \mathbb{R}$,

$$\mathbb{E}(f(Y_n)) \xrightarrow[n\to+\infty]{} \mathbb{E}(f(Y))$$

Only the law of the random variables matters. For example, if $Y \sim \mathcal{N}(0, 1)$, then one can replace it by any random variable with the same law and write :

$$Y_n \xrightarrow[n\to+\infty]{\text{law}} \mathcal{N}(0, 1)$$

In the case of $\mathbb{R}^d$-valued random variables $(Y_n)_{n\in\mathbb{N}^*}$ and $Y$, the definition is the same, taking $f : \mathbb{R}^d \to \mathbb{R}$.
# Theorem 6

$$Y _ { n } \xrightarrow [ n \to + \infty ] { \mathrm { l a w } } Y { \mathrm { ~ i f ~ a n d ~ o n l y ~ i f ~ } } \forall t \in \mathbb { R } , \quad \phi _ { Y _ { n } } ( t ) \xrightarrow [ n \to + \infty ] { } \phi _ { Y } ( t )$$

In the case of $\mathbb { R } ^ { d }$-valued random variables, the preceding theorem also holds true using for $Z = ( Z _ { 1 } , \ldots , Z _ { d } )$,

$$\phi _ { Z } ( x ) = \mathbb { E } \left( \exp \left( i x ^ { t } Z \right) \right) = \mathbb { E } \left( \exp \left( i \sum _ { j = 1 } ^ { d } x _ { j } Z _ { j } \right) \right)$$

The Gaussian laws are central because of the central limit theorem.
# Theorem 7

Let $(X_i)_{i\in\mathbb{N}^*}$ be a sequence of i.i.d. (independent, identically distributed) random variables of $L^2(\Omega, \mathcal{T}, \mathbb{P})$ (i.e., $\mathbb{E}(X_i^2) < +\infty$). Let $m = \mathbb{E}(X_i)$, $\sigma^2 = \mathbb{V}(X_i)$, and $S_n = X_1 + \cdots + X_n$, for all $n \in \mathbb{N}^*$.

$$\frac{S_n - nm}{\sqrt{n}} \xrightarrow[n \to +\infty]{\text{law}} \mathcal{N}(0, \sigma^2)$$

$$\frac{\sqrt{n}}{\sqrt{\mathbb{V}(X_1)}} \left( \frac{1}{n} \sum_{i=1}^n X_i - \mathbb{E}(X_1) \right) \xrightarrow[n \to +\infty]{\text{law}} \mathcal{N}(0, 1)$$

This is why numerous random natural phenomena can be represented in the limit by Gaussian laws.

The central limit theorem provides the speed of convergence of the empirical mean to the true mean : this convergence is the Law of Large Numbers.
# Theorem 7

Let $(X_i)_{i\in\mathbb{N}^*}$ be a sequence of i.i.d. (independent, identically distributed) random variables of $L^1(\Omega, \mathcal{T}, \mathbb{P})$ (i.e., $\mathbb{E}(|X_i|) < +\infty$). Then

$$\frac{1}{n} \sum_{i=1}^n X_i \xrightarrow[n \to +\infty]{p.s.} \mathbb{E}(X_1)$$
# Lemma 7

Let $(X_n)_{n\in\mathbb{N}^*}$ be a sequence of real-valued Gaussian random variables, $X_n \sim \mathcal{N}(m_n, \sigma_n^2)$. Assume that $m_n \xrightarrow[n\to+\infty]{n\to+\infty} m$ and $\sigma_n \to \sigma$. Then $X_n \xrightarrow[n\to+\infty]{law} X$ where $X \sim \mathcal{N}(m, \sigma^2)$.
# Definition 41

A random vector $X = (X_1, \ldots, X_d)$ of $\mathbb{R}^d$ is said to be Gaussian if

- Any linear combination of its components is a Gaussian real-valued random variable.
- $\forall a \in \mathbb{R}^d, a^t X = \sum_{i=1}^d a_i X_i$ is a Gaussian real-valued random variable.

A Gaussian law $\mu$ on $\mathbb{R}^d$ is a law of a Gaussian vector, i.e.,

$$\mu = \mathbb{P}_{(X_1, \ldots, X_d)}.$$

The components $X_i$ of a Gaussian vector $X$ are always Gaussian real-valued random variables, but the converse is false in general.
# Lemma 8

Let $X_1, \ldots, X_n$ be independent Gaussian random variables with law $\mathcal{N}(m_i, \sigma_i^2)$ for $i = 1, \ldots, n$. Let $a_1, b_1, \ldots, a_n, b_n \in \mathbb{R}$. Then $\sum_{i=1}^n (a_i X_i + b_i)$ is a Gaussian random variable whose law is given by $\mathcal{N}\left(\sum_{i=1}^n (a_i m_i + b_i), \sum_{i=1}^n (a_i \sigma_i)^2\right)$.

$$\begin{aligned} \Phi_{\sum_{j=1}^n (a_j X_j + b_j)}(t) &= \mathbb{E} e^{it \sum_{j=1}^n (a_j X_j + b_j)} = \mathbb{E} \left( \prod_{j=1}^n e^{it(a_j X_j + b_j)} \right) \\ &= \prod_{j=1}^n \mathbb{E} e^{it(a_j X_j + b_j)} = \prod_{j=1}^n e^{it b_j} \Phi_{X_j}(a_j t) \\ &= \prod_{j=1}^n e^{it b_j} e^{it a_j m_j - \frac{t^2 \sigma_j^2 a_j^2}{2}} \\ &= e^{it \sum_{j=1}^n (a_j m_j + b_j) - \frac{t^2}{2} \sum_{j=1}^n (a_j \sigma_j)^2} \end{aligned}$$
# Lemma 9

If the Gaussian real-valued random variables $X_1, \ldots, X_d$ are independent, then $(X_1, \ldots, X_d)$ is a Gaussian vector. Let $a \in \mathbb{R}^d$, then $\sum_{i=1}^d a_i X_i$ is a Gaussian real-valued random variable with law $\mathcal{N}\left(\sum_{i=1}^d a_i m_i, \sum_{i=1}^d a_i^2 \sigma_i^2\right)$.
# Lemma 10

Let $X$ be a Gaussian vector with values in $\mathbb{R}^d$.

- Mean : $m = \mathbb{E}X = (\mathbb{E}X_1, \ldots, \mathbb{E}X_d)^t$.
- Variance-covariance matrix :

$$\Sigma_X = (\text{Cov}(X_i, X_j))_{i,j} = \mathbb{E}((X - \mathbb{E}X)(X - \mathbb{E}X)^t)$$

Let $A$ be an $n \times d$ matrix.

Let $b \in \mathbb{R}^n$.

Then $Y = AX + b$ is a Gaussian vector with values in $\mathbb{R}^n$.

- Mean : $Am + b$.
- Variance-covariance matrix : $A\Sigma_X A^t$.

Let $a \in \mathbb{R}^n$, $a^t Y = a^t b + a^t AX$ is a Gaussian real-valued random variable since $X$ is a Gaussian vector.
# Theorem 8

Let $X$ be a random vector in $\mathbb{R}^d$, with mean $m$ and variance-covariance matrix $\Sigma_X$.

$X$ is a Gaussian vector if and only if its characteristic function is given by :

$$\Phi_X(u) = \mathbb{E}e^{iu^t X} = e^{iu^t m - \frac{1}{2}u^t \Sigma_X u}, \quad \forall u \in \mathbb{R}^d$$

As the characteristic function characterizes the law, a Gaussian law is completely determined by its mean $m$ and variance-covariance matrix $\Sigma_X$.
# Theorem 9

Let $m \in \mathbb{R}^d$ and $K$ be a $d \times d$, symmetric, positive semi-definite matrix.

$K$ is a positive semi-definite matrix if $x^t Kx \geq 0$ for all $x \in \mathbb{R}^d$.

Then there exists a unique Gaussian law with mean $m$ and variance-covariance matrix $K : \mathcal{N}_d(m, K)$.

To have a Gaussian law $\mathcal{N}_d(m, K)$, one needs (and it is also sufficient) for $K$ :

1. Size $d \times d$,
2. Symmetric,
3. Positive semi-definite.

Its eigenvalues are non-negative and det $K \geq 0$.
# Cholesky Decomposition

Let $K$ be a $d \times d$ symmetric, positive semi-definite matrix.

Then $K$ is diagonalizable in an orthonormal basis.

Let $P$ be the orthogonal change of basis matrix and $\lambda_1, \ldots, \lambda_d$ the eigenvalues of $K$ ($\lambda_i \ge 0$ for $i = 1, \ldots, d$).

Let $A = P\text{diag}(\sqrt{\lambda_1}, \ldots, \sqrt{\lambda_d})P^t$, $A$ is a $d \times d$ matrix.

Then $A$ is symmetric, positive semi-definite matrix such that $K = AA^t$.
# Theorem 10

Let $(X_1, \ldots, X_m, Y_1, \ldots, Y_n)$ be a Gaussian vector. Then the following assertions are equivalent.

- The two vectors $X = (X_1, \ldots, X_m)$ and $Y = (Y_1, \ldots, Y_n)$ are independent.
- For all $1 \le i \le m$, $1 \le j \le n$, $\text{Cov}(X_i, Y_j) = 0$.
- The variance-covariance matrix of $(X, Y)$ is diagonal by block :

$$\Sigma_{(X,Y)} = \begin{pmatrix} \Sigma_X & 0 \\ 0 & \Sigma_Y \end{pmatrix}$$

Be careful : the big vector $(X, Y)$ has to be a Gaussian vector and not $X$ and $Y$ separately.

Can be extended to $k$ blocks.
# Theorem 11

Assume that $X = (X_1, \ldots, X_d)$ is a Gaussian vector. Then

- The $X_1, \ldots, X_d$ are independent if and only if the variance-covariance matrix $\Sigma_X$ is diagonal, i.e., for all $1 \le i < j \le d$, $\text{Cov}(X_i, X_j) = 0$.
- The real-valued random variables $X_1, \ldots, X_d$ are independent with law given by $\mathcal{N}(m_i, \sigma_i^2)$ if and only if $X = (X_1, \ldots, X_d)$ is a Gaussian vector with mean $m_X = (m_1, \ldots, m_d)$ and variance-covariance matrix $\Sigma_X$:

$$\Sigma_X = \begin{pmatrix} \sigma_1^2 & 0 & \ldots & 0 \\ 0 & \sigma_2^2 & \ldots & 0 \\ \vdots & \vdots & \ddots & \vdots \\ 0 & 0 & \ldots & \sigma_d^2 \end{pmatrix}$$
# Conditional Expectation :Definition 50

As usual, let $$(\Omega, \mathcal{T}, \mathbb{P})$$ be a probability space.

Let $$A, B \in \mathcal{T}$$ with $$\mathbb{P}(B) > 0$$. The probability of $$B$$ conditionally to $$A$$ is given by

$$\mathbb{P}(A \mid B) = \frac{\mathbb{P}(A \cap B)}{\mathbb{P}(B)}$$

Let $$X$$ be a random variable and $$B \in \mathcal{T}$$, $$\mathbb{P}(B) > 0$$. The conditional expectation of $$X$$ knowing $$B$$ is the number

$$\mathbb{E}(X \mid B) = \frac{\mathbb{E}(X1_B)}{\mathbb{P}(B)}$$

With $$X = 1_A$$, the two definitions coincide :

$$\mathbb{E}(1_A \mid B) = \frac{\mathbb{E}(1_A 1_B)}{\mathbb{P}(B)} = \frac{\mathbb{E}(1_{A \cap B})}{\mathbb{P}(B)} = \frac{\mathbb{P}(A \cap B)}{\mathbb{P}(B)} = \mathbb{P}(A \mid B)$$

The difference between expectation and conditional expectation is information. It can be an event (here $$B$$) but also a random
# Definition 51

Let $X$ be a real-valued random variable and $Y$ a random variable taking a countable number of values. The conditional expectation of $X$ knowing $Y$, $\mathbb{E}(X \mid Y)$ is a random variable given by

$$\mathbb{E}(X \mid Y)(\omega) = \phi(Y(\omega)), \quad \forall \omega \in \Omega$$

where

$$\phi : \mathbb{R} \to \mathbb{R}$$

$$y \mapsto \begin{cases} \mathbb{E}(X \mid Y = y) = \frac{\mathbb{E}(X\mathbf{1}_{\{Y=y\}})}{\mathbb{P}(\{Y=y\})} & \text{if } \mathbb{P}(\{Y = y\}) > 0 \\ 0 & \text{else} \end{cases}$$

$$\mathbb{E}(X \mid Y)(\omega) = \phi(Y(\omega)) = \mathbb{E}(X \mid \{Y = Y(\omega)\}) = \frac{\mathbb{E}\left(X\mathbf{1}_{\{Y=Y(\omega)\}}\right)}{\mathbb{P}(\{Y = Y(\omega)\})}$$

Let $\Omega = \{1, 2, \ldots, 6\}$, $\mathcal{T} = \mathcal{P}(\Omega)$, and $\mathbb{P}(\{\omega\}) = \frac{1}{6}$, $\forall \omega \in \Omega$. We define the random variables
# Definition 51

Let $\omega \in \{1, 3, 5\}$, then $Y(\omega) = 1$ and

$$\mathbb{E}(X \mid Y)(\omega) = \mathbb{E}(X \mid Y = Y(\omega)) = \mathbb{E}(X \mid Y = 1) = \frac{\mathbb{E}\left(X\mathbf{1}_{\{Y=1\}}\right)}{\mathbb{P}(\{Y=1\})} = \frac{\frac{1}{6}(1+3+5)}{\frac{3}{6}} = 3$$

If $\omega \in \{2, 4, 6\}$, then $Y(\omega) = 0$ and

$$\mathbb{E}(X \mid Y)(\omega) = \mathbb{E}(X \mid Y = Y(\omega)) = \mathbb{E}(X \mid Y = 0) = \frac{\mathbb{E}\left(X\mathbf{1}_{\{Y=0\}}\right)}{\mathbb{P}(\{Y=0\})} = \frac{\frac{1}{6}(2+4+6)}{\frac{3}{6}} = 4$$
# Conditional Expectation with Respect to a Sigma-Algebra

Let $$(\Omega, \mathcal{T}, \mathbb{P})$$ be a probability space. Let $$\mathcal{G}$$ be a sub-sigma algebra of $$\mathcal{T}$$ ($$\mathcal{G} \subset \mathcal{T}$$ and $$\mathcal{G}$$ is a sigma-algebra on $$\Omega$$).

This means that $$\mathcal{G}$$ contains part of the total information given by $$\mathcal{T}$$.

We are looking for a summary of a random variable $$Y$$ defined on $$(\Omega, \mathcal{T}, \mathbb{P})$$ if we know only the information of $$\mathcal{G}$$.

This is the conditional expectation of $$Y$$ knowing $$\mathcal{G}$$.
# Definition 52

Let $Y \in L^1(\Omega, \mathcal{T}, \mathbb{P})$. The conditional expectation of $Y$ knowing $\mathcal{G}$ is any random variable $Z$ such that

1. $Z$ is $\mathcal{G}$-measurable,
2. $\forall G \in \mathcal{G}, \mathbb{E}(1_G Y) = \mathbb{E}(1_G Z)$.

Such a random variable $Z$ exists and is unique, up to a $\mathbb{P}$-a.s. equality. One writes $Z = \mathbb{E}(Y \mid \mathcal{G})$.

If $\Omega$ is finite and such that $\mathbb{P}(\omega) > 0$ for all $\omega \in \Omega$, then $\mathbb{E}(Y \mid \mathcal{G})$ is unique!

When $Y \in L^2(\Omega, \mathcal{T}, \mathbb{P})$, the conditional expectation of $Y$ can be seen as an orthogonal projection of $Y$ from the Hilbert space $L^2(\Omega, \mathcal{T}, \mathbb{P})$ on its (kind of) sub-space $L^2(\Omega, \mathcal{G}, \mathbb{P})$.

For $L^1(\Omega, \mathcal{F}, \mathbb{P})$, either use $L^1$ density of $L^2(\Omega, \mathcal{T}, \mathbb{P})$ in $L^1(\Omega, \mathcal{T}, \mathbb{P})$ or Radon-Nikodym Theorem (see later).
# Definition 52

In general, there is no uniqueness of the conditional expectation : if Z and Z' satisfy (i) and (ii), then Z = Z'P-a.s.

Instead of (ii), one can assume that for all G-measurable random variable X, E(XY) = E(XZ).

The conditional expectation is a random variable : if an experiment is done and ω ∈ Ω is realized, then E(Y | G)(ω) is the best estimate of Y(ω) (in the sense of the quadratic mean) when one knows the information of G.

Let Y = (Y₁, ..., Y_d) be a R^d integrable random vector, then

$$\mathbb{E}(Y \mid \mathcal{G}) := (\mathbb{E}(Y_1 \mid \mathcal{G}), \dots, \mathbb{E}(Y_d \mid \mathcal{G}))$$

Let X : (Ω, T, P) → (E, E) be a random variable, then

$$\mathbb{E}(Y \mid X) := \mathbb{E}(Y \mid \sigma(X))$$
# Properties of Conditional Expectation

Let X and Y be two integrable real-valued random variables. Then

1 In average, Y and E(Y | G) are the same :

E(E(Y | G)) = E(Y).

2 Taking out what is known : if Y is G-measurable, then the value of Y is known once G is known : E(Y | G) = YP-a.s.

3 Similarly, if X is G-measurable (but not Y) and E|XY| < +∞, then

E(XY | G) = XE(Y | G)P-a.s.

4 Let a ∈ R, E(aX + Y | G) = aE(X | G) + E(Y | G)P-a.s.

5 Let Y ≥ 0P-a.s., E(Y | G) ≥ 0P-a.s.
# Example of Proofs

- For 1. Take $G = \Omega$.
- For 2. As $Y$ is $\mathcal{G}$-measurable and $\forall G \in \mathcal{G}$, $\mathbb{E}(Y\mathbf{1}_G) = \mathbb{E}(Y\mathbf{1}_G)$, $\mathbb{E}(Y \mid \mathcal{G}) = Y\mathbb{P}$-a.s.
- For 4. Let $G \in \mathcal{G}$,

$$\begin{array}{l} \mathbb{E}\left(\left(a\mathbb{E}(X \mid \mathcal{G}) + b\mathbb{E}(Y \mid \mathcal{G})\right)\mathbf{1}_G\right) = a\mathbb{E}\left(\mathbb{E}(X \mid \mathcal{G})\mathbf{1}_G\right) + b\mathbb{E}\left(\mathbb{E}(Y \mid \mathcal{G})\right) \\ = a\mathbb{E}\left(X\mathbf{1}_G\right) + b\mathbb{E}\left(Y\mathbf{1}_G\right) \\ = \mathbb{E}\left(\left(aX + bY\right)\mathbf{1}_G\right) \end{array}$$

and as $a\mathbb{E}(X \mid \mathcal{G}) + b\mathbb{E}(Y \mid \mathcal{G})$ is $\mathcal{G}$-measurable, we get that $\mathbb{E}(aX + bY \mid \mathcal{G}) = a\mathbb{E}(X \mid \mathcal{G}) + b\mathbb{E}(Y \mid \mathcal{G})\mathbb{P}$-a.s.
# Further Properties

Let X and Y be two integrable real-valued random variables. Then

1 If X is G-measurable and E|X E(Y | G)| < +∞, then

$$\mathbb{E}(X\mathbb{E}(Y \mid \mathcal{G})) = \mathbb{E}(XY)$$

2 Let H be a sub-sigma algebra of G, then the chaining rule or tower law is

$$\mathbb{E}(\mathbb{E}(Y \mid \mathcal{G}) \mid \mathcal{H}) = \mathbb{E}(Y \mid \mathcal{H})\mathbb{P}\text{-a.s.}$$

3 In words, when taking iterated expectations with respect to several levels of information, it is enough to take a single conditional expectation with respect to the smallest set of information available.
# Example of the Chaining Rule Property

Let $\Omega = \{1, 2, \ldots, 6\}$ and $\forall \omega \in \Omega, \mathbb{P}(\{\omega\}) = \frac{1}{6}$. Let

$$X(\omega) = \omega, \quad Y(\omega) = \begin{cases} 1 & \text{if } \omega \text{ is odd} \\ 0 & \text{if } \omega \text{ is even} \end{cases}, \quad Z = \begin{cases} 1 & \text{if } X \in \{1, 3\} \\ 2 & \text{if } X = 5 \\ 3 & \text{if } X \in \{2, 4\} \\ 4 & \text{if } X = 6 \end{cases}$$

If $Z$ is known, $Y$ is also known and thus $\sigma(Y) \subset \sigma(Z)$.

$$\mathbb{E}(X \mid Z)(\omega) = \begin{cases} \frac{\mathbb{E}(X\mathbf{1}_{\{Z=1\}})}{\mathbb{P}(\{Z=1\})} = \frac{\frac{1}{6}(1+3)}{\frac{2}{6}} = 2 & \text{if } \omega \in \{1, 3\}, Z(\omega) = 1 \\ \frac{\mathbb{E}(X\mathbf{1}_{\{Z=2\}})}{\mathbb{P}(\{Z=2\})} = \frac{\frac{1}{6} \cdot 5}{\frac{1}{6}} = 5 & \text{if } \omega = 5, Z(\omega) = 2 \\ \frac{\mathbb{E}(X\mathbf{1}_{\{Z=3\}})}{\mathbb{P}(\{Z=3\})} = \frac{\frac{1}{6}(2+4)}{\frac{2}{6}} = 3 & \text{if } \omega \in \{2, 4\}, Z(\omega) = 3 \\ \frac{\mathbb{E}(X\mathbf{1}_{\{Z=4\}})}{\mathbb{P}(\{Z=4\})} = \frac{\frac{1}{6} \cdot 6}{\frac{1}{6}} = 6 & \text{if } \omega = 6, Z(\omega) = 4 \end{cases}$$
# Example of the Chaining Rule Property

$$\mathbb{E}(\mathbb{E}(X \mid Z) \mid Y)(\omega) = \begin{cases} \frac{\mathbb{E}(\mathbb{E}(X|Z)\mathbf{1}_{\{Y=1\}})}{\mathbb{P}(\{Y=1\})} = \frac{\frac{2}{6}\cdot 2 + \frac{1}{6}\cdot 5}{\frac{3}{6}} = 3 & \text{if } \omega \in \{1, 3, 5\}, \\ \frac{\mathbb{E}(\mathbb{E}(X|Z)\mathbf{1}_{\{Y=0\}})}{\mathbb{P}(\{Y=0\})} = \frac{\frac{2}{6}\cdot 3 + \frac{1}{6}\cdot 6}{\frac{3}{6}} = 4 & \text{if } \omega \in \{2, 4, 6\}, \end{cases}$$

But we have seen that $\mathbb{E}(X \mid Y) = 3$ on $\{Y = 1\}$ and 4 on $\{Y = 0\}$, thus $\mathbb{E}(\mathbb{E}(X \mid Z) \mid Y) = \mathbb{E}(X \mid Y)$.
# Conditional Expectation with Independence

Let X and Y be two integrable real-valued random variables.

- Let H be a sub-sigma algebra of T and σ(σ(Y) ∪ H) the smallest sigma-algebra containing σ(Y) and H. If G and σ(σ(Y) ∪ H) are independent, then

$$\mathbb{E}(Y \mid \sigma(\mathcal{H} \cup \mathcal{G})) = \mathbb{E}(Y \mid \mathcal{H})\mathbb{P}\text{-a.s.}$$

- In particular, if σ(Y) and G are independent, then knowing G is useless to predict the value of Y :

$$\mathbb{E}(Y \mid \mathcal{G}) = \mathbb{E}(Y)\mathbb{P}\text{-a.s.}$$
# Conditional Expectation with Independence

Let $X : (\Omega, \mathcal{T}, \mathbb{P}) \to (E, \mathcal{E})$ be a random variable independent of $\mathcal{G}$ and assume that $Y$ is $\mathcal{G}$-measurable. Let $\Phi : \mathbb{R} \times E$ be $\mathcal{B}(\mathbb{R}) \otimes \mathcal{E}$-measurable, which is non-negative or bounded. Then $\varphi : \mathbb{R} \to \mathbb{R}$

$$\forall y \in \mathbb{R}, \quad \varphi(y) := \mathbb{E}\Phi(y, X)$$

is measurable and

$$\mathbb{E}(\Phi(Y, X) \mid \mathcal{G})(\omega) = \varphi(Y(\omega)) \text{ for } \mathbb{P} \text{ a.e. } \omega$$
# Jensen Inequality

If $\Phi : \mathbb{R} \to \mathbb{R}$ is a convex function such that $\mathbb{E}|\Phi(Y)| < \infty$, then

$$\Phi(\mathbb{E}(Y \mid \mathcal{G})) \leq \mathbb{E}(\Phi(Y) \mid \mathcal{G}) \quad \mathbb{P}\text{-a.s.}$$
# Theorem 12

If $\mathcal{G} = \sigma(A_1, \ldots, A_n)$ where $A_1, \ldots, A_n$ is a partition of $\Omega$, then

$$\mathbb{E}(X \mid \mathcal{G}) = \sum_{j=1}^{n} \frac{\mathbb{E}(X 1_{A_j})}{\mathbb{P}(A_j)} 1_{A_j} \mathbb{P}\text{-a.s.}$$

Indeed, as $\mathbb{E}(X \mid \mathcal{G})$ is $\mathcal{G}$-measurable, there exist $\lambda_1, \ldots, \lambda_n \in \mathbb{R}$ such that $\mathbb{E}(X \mid \mathcal{G}) = \sum_{j=1}^{n} \lambda_j 1_{A_j}$. Using the definition of the conditional expectation

$$\mathbb{E}(1_{A_i} X) = \mathbb{E}(1_{A_i} \mathbb{E}(X \mid \mathcal{G})) = \sum_{j=1}^{n} \lambda_j \mathbb{E}(1_{A_j} 1_{A_i}) = \lambda_i \mathbb{P}(A_i)$$
# Example of Conditional Expectation with Partition

Price process example for two periods $\Omega = \{UU, UD, DU, DD\}$.

The sets $A_U = \{UU, UD\}$ and $A_D = \{DU, DD\}$ form a partition of $\Omega$. We set $\mathcal{T}_1 = \{\emptyset, A_U, A_D, \Omega\}$.

Let $S_2$ be the price of the stock at time 2 :

$$S_2(UU) = s_0 u^2, \quad S_2(UD) = S_2(DU) = s_0 ud, \quad S_2(DD) = s_0 d^2$$

$$\mathbb{E}(S_2 \mid \mathcal{T}_1) = \frac{\mathbb{E}(S_2 1_{A_U})}{\mathbb{P}(A_U)} 1_{A_U} + \frac{\mathbb{E}(S_2 1_{A_D})}{\mathbb{P}(A_D)} 1_{A_D} \mathbb{P}\text{-a.s.}$$

$$\mathbb{P}(A_U) = p^2 + p(1-p) = p \text{ and } \mathbb{P}(A_D) = (1-p)^2 + p(1-p) = 1-p$$
# Example of Conditional Expectation with Partition

$$\mathbb{E}\left(S_{2}1_{A_{U}}\right)=p^{2}s_{0}u^{2}+p(1-p)s_{0}ud=ps_{0}u(pu+(1-p)d)$$

$$\mathbb{E}\left(S_{2}1_{A_{D}}\right)=(1-p)^{2}s_{0}d^{2}+p(1-p)s_{0}ud=(1-p)s_{0}d(pu+(1-p)d)$$

$$\mathbb{E}\left(S_{2}\mid\mathcal{T}_{1}\right)=(pu+(1-p)d)\left(s_{0}u1_{A_{U}}+s_{0}d1_{A_{D}}\right)\mathbb{P}\text{-a.s.}=(pu+(1-p)d)S_{1}\mathbb{P}$$
# Conditional Expectation with Density :Theorem 13

Let $$(\Omega, \mathcal{T}, \mathbb{P})$$ be a probability space.

Let $$X = (X_1, \ldots, X_p)$$ and $$Y = (Y_1, \ldots, Y_k)$$ be two random vectors such that $$(X, Y)$$ admits a density

$$f(x, y) = f(x_1, \ldots, x_p, y_1, \ldots, y_k)$$ with respect to the Lebesgue measure on $$\mathbb{R}^{p+k}$$. Then the law of $$Y$$ knowing

$$\{X = x\} = \{X_1 = x_1, \ldots, X_p = x_p\}$$ admits a density with respect to the Lebesgue measure on $$\mathbb{R}^k$$. Let $$f_X(x) = \int_{\mathbb{R}^k} f(x, y) \, dy$$ be the marginal law of $$X$$, then if $$f_X(x) > 0$$,

$$f_Y^{X=x}(y) = f_Y^{X=(x_1, \ldots, x_p)}(y_1, \ldots, y_k) = \frac{f(x_1, \ldots, x_p, y_1, \ldots, y_k)}{f_X(x_1, \ldots, x_p)}$$

We choose for $$f_Y^{X=x}(y)$$ when $$f_X(x) = 0$$ any density of probability. The interpretation is the following (we take $$p = k = 1$$) :

$$f_Y^{X=x}(y)\Delta y = \frac{f(x, y)\Delta x \Delta y}{f_X(x)\Delta x} \sim \frac{\mathbb{P}(\{x \leq X \leq x + \Delta x, y \leq Y \leq y + \Delta y\})}{\mathbb{P}(\{x \leq X \leq x + \Delta x\})}$$
# Conditional Expectation with Density :Theorem 14

For any positive or bounded $g : \mathbb{R}^{p+k} \to \mathbb{R}$ :

$$\mathbb{E}(g(X, Y) \mid X = x) = \int_{\mathbb{R}^k} g(x, y) f_Y^{X=x}(y) \, dy$$

$$\mathbb{E}(g(X, Y) \mid X)(\omega) = \mathbb{E}(g(X, Y) \mid X = X(\omega)) = \int_{\mathbb{R}^k} g(X(\omega), y) f_Y^{X=X(\omega)}$$
# Conditional Expectation with Density :Theorem 15

Let \( (Y_{1},\ldots,Y_{n},X) \) be a Gaussian vector. Then there exist \( \Lambda=(\lambda_{1},\ldots,\lambda_{n})^{t}\in\mathbb{R}^{n} \) such that

\[
\mathbb {E} (X \mid \sigma (Y _ {1}, \dots , Y _ {n})) = \mathbb {E} (X) + \sum_ {j = 1} ^ {n} \lambda_ {j} (Y _ {j} - \mathbb {E} (Y _ {j})) = \mathbb {E} (X) + \Lambda^ {t} (Y - \mathbb {E} (Y))
\]

where

\[
\Lambda = \Sigma_ {Y} ^ {- 1} \left( \begin{array}{c} \operatorname{Cov} (X, Y _ {1}) \\ \vdots \\ \operatorname{Cov} (X, Y _ {n}) \end{array} \right)
\]
# Conditional Expectation with Density : Theorem 15

We have already said that a random variable X which is σ(Y) measurable can be written as X = φ(Y) where φ is measurable. Thus

$$\mathbb{E}(X \mid \sigma(Y_1, \dots, Y_n)) = \varphi(Y_1, \dots, Y_n)$$

The linear combination form comes from projection properties : the conditional expectation coincides with the linear regression of X over (Y₁, …, Yₙ).

Computation of the λᵢ. Let Z = 𝔼(X | σ(Y₁, …, Yₙ)). Then by definition of the conditional expectation ∀i ∈ {1, …, n}, since Yᵢ − 𝔼(Yᵢ) is σ(Y₁, …, Yₙ)-measurable :

$$\mathbb{E}(Z(Y_i - \mathbb{E}(Y_i))) = \mathbb{E}(X(Y_i - \mathbb{E}(Y_i)))$$

$$= \mathbb{E}((X - \mathbb{E}(X))(Y_i - \mathbb{E}(Y_i))) + \mathbb{E}(X)\mathbb{E}(Y_i - \mathbb{E}(Y_i)) = \text{Cov}(X, Y_i)$$
# Conditional Expectation with Density : Theorem 15

Moreover,

$$\mathbb{E}(Z(Y_i - \mathbb{E}(Y_i))) = \mathbb{E} \left( \left( \mathbb{E}(X) + \sum_{j=1}^n \lambda_j (Y_j - \mathbb{E}(Y_j)) \right) (Y_i - \mathbb{E}(Y_i)) \right) =$$

Let $\Sigma_Y$ be the variance-covariance matrix of $Y$. The last equation implies that

$$\Sigma_Y \begin{pmatrix} \lambda_1 \\ \vdots \\ \lambda_n \end{pmatrix} = \begin{pmatrix} \mathbb{E}(Z(Y_1 - \mathbb{E}(Y_1))) \\ \vdots \\ \mathbb{E}(Z(Y_n - \mathbb{E}(Y_n))) \end{pmatrix} = \begin{pmatrix} \text{Cov}(X, Y_1) \\ \vdots \\ \text{Cov}(X, Y_n) \end{pmatrix}$$

Thus,

$$\begin{pmatrix} \lambda_1 \\ \vdots \\ \lambda_n \end{pmatrix} = \Sigma_Y^{-1} \begin{pmatrix} \text{Cov}(X, Y_1) \\ \vdots \\ \text{Cov}(X, Y_n) \end{pmatrix}$$
# Change of Law

In financial mathematics, there are different laws of probability that coexist.

For example, there is the real world or physical probability measure, which we encounter every day for financial data. There is also another probability measure which is used for pricing purposes and which is called the risk-neutral or pricing or even martingale measure.

So it is important to be able to change measures and transpose some problem subject to the real world measure into an equivalent problem formulated as a martingale under a different probability measure.

It is also important to consider measures that agree under the events which are impossible or sure.
# Definition 57

Let $(\Omega, \mathcal{T})$ be a measurable space.

A probability measure $\mathbb{Q}$ is absolutely continuous with respect to $\mathbb{P}$ if and only if for all $A \in \mathcal{T}$ such that $\mathbb{P}(A) = 0$, then $\mathbb{Q}(A) = 0$.

This is denoted by $\mathbb{Q} \ll \mathbb{P}$.

All impossible (resp. sure) events under $\mathbb{P}$ remain impossible (resp. sure) under $\mathbb{Q}$.

If $\mathbb{P} \ll \mathbb{Q}$ and $\mathbb{Q} \ll \mathbb{P}$, then $\mathbb{P}$ and $\mathbb{Q}$ are equivalent. This is denoted by $\mathbb{P} \sim \mathbb{Q}$. The two measures have the same impossible (resp. sure) events.

Probability measures $\mathbb{P}$ and $\mathbb{Q}$ are singular if and only if there exists $N \in \mathcal{T}$ such that $\mathbb{P}(N) = 0$ and $\mathbb{Q}(N^c) = 0$.

This is denoted by $\mathbb{P} \perp \mathbb{Q}$.

Example : $\lambda \perp \delta_0$ where $\lambda$ is the Lebesgue measure on $\mathbb{R}$ and $\delta_0$ the Dirac measure at $0$, $\delta_0(A) = \mathbb{1}_A(0)$ for all $A \in \mathcal{B}(\mathbb{R})$.

$\lambda(\{0\}) = 0$ and $\delta_0(\{0\}^c) = 0$.
# Radon-Nikodym Theorem

Let $\mathbb{Q}$ be a probability measure with density $f$ with respect to $\mathbb{P}$, then $\mathbb{Q} \ll \mathbb{P}$. Indeed, let $A \in \mathcal{T}$ such that $\mathbb{P}(A) = 0$, then

$$\mathbb{Q}(A) = \int 1_A f \, d\mathbb{P} = 0.$$
# Theorem 16

Let $\mathbb{Q}$ and $\mathbb{P}$ be two probability measures on $(\Omega, \mathcal{T})$. Then the following assertions are equivalent :

1. $\mathbb{Q} \ll \mathbb{P}$.
2. There exists $f \in \mathcal{L}_{\mathbb{R}^+}^1(\Omega, \mathcal{T}, \mathbb{P})$ (unique a.s.) such that $\mathbb{Q}$ has a density function $f$ with respect to $\mathbb{P}$, i.e., $\mathbb{Q} = f \cdot \mathbb{P}$ or $\mathbb{Q}(A) = \int 1_A f \, d\mathbb{P}$.

$f$ is called the Radon-Nikodym derivative.

With discrete distributions, then the theorem is just

$$\mathbb{Q}(\omega) = f(\omega)\mathbb{P}(\omega).$$
# Example

Let $\Omega = \{U, D\}$.

- Case 1: $\mathbb{P}(\{U\}) = p = 1 - \mathbb{P}(\{D\})$ with $0 < p < 1$.
- Case 2: "fair" game $\mathbb{Q}(\{U\}) = \mathbb{Q}(\{D\}) = \frac{1}{2}$.

The change of measure between $\mathbb{P}$ and $\mathbb{Q}$ is to go from the world of case 1 $(\Omega, \mathcal{P}(\Omega), \mathbb{P})$ to the world of case 2 $(\Omega, \mathcal{P}(\Omega), \mathbb{Q})$ one has to:

1. Weight the likelihood of event $\{U\}$ from $f(U) = \frac{\mathbb{Q}(\{U\})}{\mathbb{P}(\{U\})} = \frac{1}{2p}$.
2. Weight the likelihood of event $\{D\}$ from

$$f(D) = \frac{\mathbb{Q}(\{D\})}{\mathbb{P}(\{D\})} = \frac{1}{2(1-p)}.$$

Thus $\mathbb{Q}(\omega) = \frac{\mathbb{Q}(\omega)}{\mathbb{P}(\omega)}\mathbb{P}(\omega)$.

Note that since $0 < p < 1$, we have $\mathbb{P}(\{U\}) \neq 0$ and $\mathbb{P}(\{D\}) \neq 0$ and hence $\mathbb{P}$ and $\mathbb{Q}$ are equivalent.
# Bayes Formula : Theorem 17

Let $$(\Omega, \mathcal{T}, \mathbb{P})$$ be a probability space, $$\mathcal{G}$$ be a sub-sigma-algebra of $$\mathcal{T}$$ and $$L : \Omega \to \mathbb{R}_+^*$$ a random variable such that $$\mathbb{E}_{\mathbb{P}}(L) = 1$$.

Let $$\mathbb{Q}$$ be defined by $$\mathbb{Q}(A) = \mathbb{E}_{\mathbb{P}}(\mathbf{1}_A L)$$ for all $$A \in \mathcal{T}$$.

$$\mathbb{Q}$$ is a probability measure with density $$L$$ with respect to $$\mathbb{P}$$.

Notation : $$L = \frac{d\mathbb{Q}}{d\mathbb{P}}$$ or $$\mathbb{Q} = L \cdot \mathbb{P}$$.

For any real-valued random variable (or $$\mathbb{R}^d$$ random vector) $$X$$ on $$\Omega$$,

$$\mathbb{E}_{\mathbb{Q}}(X \mid \mathcal{G}) = \frac{\mathbb{E}_{\mathbb{P}}(XL \mid \mathcal{G})}{\mathbb{E}_{\mathbb{P}}(L \mid \mathcal{G})} \quad \mathbb{P}\text{-a.s.}$$
# Random Process, Martingale and Stopping Time

Let $$(\Omega, \mathcal{F}, \underline{\mathcal{F}}, \mathbb{P})$$ be a filtered probability space : the filtration $$\underline{\mathcal{F}} = (\mathcal{F}_t)_{t \in I}$$ with a time interval $$I = [0, T]$$ or $$\mathbb{R}_+$$. In finance, usually $$\mathcal{F} = \mathcal{F}_T$$ if $$I = [0, T]$$ : all is known at this end !

A random process indexed by $$I$$ is a family of random variables $$(X_t)_{t \in I}$$.

We start with random processes with values in $$\mathbb{R}$$, i.e., the $$X_t$$ are real-valued random variables but all can be written for processes taking values in $$\mathbb{R}^d$$.

A random process can be considered from a pathwise point of view. Let $$\mathbb{R}^I$$ be the set of functions $$f : I \to \mathbb{R}$$. Then a random process can be seen as a function from $$\Omega$$ to $$\mathbb{R}^I : \omega \mapsto (t \mapsto X_t(\omega))$$.

A random process is ... a random function and the way the process $$X$$ takes (randomly) values in $$\mathbb{R}^I$$ is called the law of $$X$$.
# Random Process, Martingale and Stopping Time

The path is defined for $\omega \in \Omega$ fixed, i.e., it is $t \mapsto X_t(\omega)$.

The process can also be seen as a function from $I \times \Omega \to \mathbb{R}$.

The examples will be Poisson processes, martingales and in particular the Brownian motion.

In order to know the law of $X$ it is enough to know the law of the finite-dimensional random vectors $(X_{t_1}, \ldots, X_{t_n})$ with $n \in \mathbb{N}^*$ and $(t_1, \ldots, t_n) \in I^n$.

The Brownian motion is a continuous process.

A random process $(X_t)_{t \in I}$ is continuous if all the paths are continuous, i.e., $\forall \omega \in \Omega$, $t \mapsto X_t(\omega)$ is continuous on $I$ (for any fixed $\omega$, continuity in time of $X_t(\omega)$).

Let $C^0(I, \mathbb{R})$ be the set of continuous functions $f : I \to \mathbb{R}$. A continuous random process is a function from $\Omega$ to $C^0(I, \mathbb{R})$.
# Definition 60

Two random processes $$(X_t)_{0 \leq t \leq T}$$ and $$(Y_t)_{0 \leq t \leq T}$$ are independent if and only if $$(X_{t_1}, \ldots, X_{t_n}) \perp (Y_{s_1}, \ldots, Y_{s_m})$$, for all $$n, t_1 < \ldots < t_n$$ and all $$m, s_1 < \ldots < s_m$$.
# Definition 61

A random process $(X_t)_{0 \leq t \leq T}$ valued in $\mathbb{R}^d$ is adapted if $X_t$ is $\mathcal{F}_t$-measurable for all $0 \leq t \leq T$.

In finance, we assume the "usual conditions". The sigma-algebra is complete (contains all the negligible sets) and right-continuous, i.e.,

$$\mathcal{F}_t = \bigcap_{s > t} \mathcal{F}_s$$

The natural filtration $\mathbb{F}^X$ associated to a random process $X$ is

$$\mathcal{F}_t^X = \sigma(X_s, s \leq t)$$
# Definition 62

A real-valued random process $(X_t)_{0 \leq t \leq T}$ is $(\mathbb{F}, \mathbb{P})$-

- sub-martingale
- martingale
- super-martingale

if and only if

- $X_t$ is $\mathcal{F}_t$-measurable, for all $0 \leq t \leq T$,
- $\mathbb{E}|X_t| < +\infty$, for all $0 \leq t \leq T$ : this is a standard integrability condition.
- For all $s, t \in [0, T], s \leq t, \mathbb{P}$-a.s.

$$\begin{cases} \mathbb{E}(X_t \mid \mathcal{F}_s) \geq X_s & (\text{sub-martingale}) \\ \mathbb{E}(X_t \mid \mathcal{F}_s) = X_s & (\text{martingale}) \\ \mathbb{E}(X_t \mid \mathcal{F}_s) \leq X_s & (\text{super-martingale}) \end{cases}$$

The definition depends, of course, on the filtration $\mathbb{F}$ and the probability measure $\mathbb{P}$.
# Definition 62

Only for a martingale, the last condition is equivalent to for all
$$0 \leq t \leq T$$

$$X_t = \mathbb{E}(X_T \mid \mathcal{F}_t) \quad \mathbb{P}\text{-a.s.}$$

Note that using the tower property

$$\begin{cases} \mathbb{E}(X_t) \geq \mathbb{E}(X_s), & \text{a sub-martingale is increasing in mean} \\ \mathbb{E}(X_t) = \mathbb{E}(X_s), & \text{a martingale is constant in mean} \\ \mathbb{E}(X_t) \leq \mathbb{E}(X_s), & \text{a super-martingale is decreasing in mean} \end{cases}$$

A martingale is a driftless process driven uniquely by noise.
# Definition 62

If $X$ is a "$x$-martingale" with respect to $\mathbb{F}$ on $(\Omega, \mathcal{F}, \mathbb{P})$, then $X$ is a "$x$-martingale" with respect to its own filtration $\mathbb{F}^X$ defined by

$$\mathcal{F}_t^X = \sigma(X_s, s \leq t)$$

- $X_t$ is $\mathcal{F}_t^X$-measurable, for all $0 \leq t \leq T$.

Take the case of a martingale. For all $s, t \in [0, T]$, $s \leq t$, as $\mathcal{F}_s^X \subset \mathcal{F}_s$, using the tower law $\mathbb{P}$-a.s.

$$\begin{aligned} \mathbb{E}(X_t \mid \mathcal{F}_s^X) &= \mathbb{E}(\mathbb{E}(X_t \mid \mathcal{F}_s) \mid \mathcal{F}_s^X) \\ &= \mathbb{E}(X_s \mid \mathcal{F}_s^X) = X_s \end{aligned}$$
# Theorem 18

Let $(X_t)_{t\geq 0}$ be an $\underline{\mathcal{F}}$-adapted continuous martingale such that $\mathbb{E}(X_t^2) < \infty$. We define the running maximum of $(X_t)_{t\geq 0}$

$$X_t^* := \sup_{0\leq s\leq t} |X_s|$$

Doob's Inequalities :

- For all $t > 0$ and $\lambda > 0$, $\mathbb{P}(X_t^* \geq \lambda) \leq \frac{1}{\lambda^2}\mathbb{E}(X_t^2)$.
- For all $t > 0$, $\mathbb{E}((X_t^*)^2) \leq 4\mathbb{E}(X_t^2)$.

Let $(\Omega, \mathcal{F}, \underline{\mathcal{F}}, \mathbb{P})$ be a filtered probability space.
# Definition 64

A random variable $\tau : (\Omega, \mathcal{F}) \to \mathbb{R} \cup \{\infty\}$ is a $\underline{\mathcal{F}}$-stopping time if and only if

$$\forall t \in \mathbb{R}, \quad \{\tau \leq t\} \in \mathcal{F}_t$$

The typical example is the time of entry in some set $A$.
# Theorem 19

Let $(X_t)_{t \geq 0}$ be an $\underline{\mathcal{F}}$-adapted $\mathbb{R}^d$-valued random process and $A \in \mathcal{B}(\mathbb{R}^d)$. Then

$$\tau_A = \inf\{s \geq 0 \text{ s.t. } X_s \in A\} \quad (\text{convention : } \inf \emptyset = +\infty)$$

is a $\underline{\mathcal{F}}$-stopping time.
# Definition 66

Let $(X_t)_{t \geq 0}$ be an $\underline{\mathcal{F}}$-adapted continuous process and $\tau$ some $\underline{\mathcal{F}}$-stopping time, then $X^\tau = (X_t^\tau)_{t \geq 0}$ the stopped process in $\tau$ is defined by $t \in \{0, \ldots, T\}, \omega \in \Omega$

$$X_t^\tau(\omega) = X_{t \wedge \tau(\omega)}(\omega) = \begin{cases} X_t(\omega) & \text{on } \{t \leq \tau(\omega)\} \\ X_{\tau(\omega)}(\omega) & \text{on } \{t > \tau(\omega)\} \end{cases}$$
# Theorem 20

Let $(X_t)_{t \geq 0}$ be an $\underline{\mathcal{F}}$-adapted continuous process and $\tau$ some $\underline{\mathcal{F}}$-stopping time such that $\mathbb{P}(\tau \leq T) = 1$ for $T \in \mathbb{R}$.

- If $X$ is an $(\underline{\mathcal{F}}, \mathbb{P})$-martingale then

$$\mathbb{E}(X_\tau \mid \mathcal{F}_t) = X_{t \wedge \tau}, \quad \mathbb{P}\text{-a.s.}$$

- If $X$ is an $(\underline{\mathcal{F}}, \mathbb{P})$-super-martingale (resp. sub-martingale) then $\mathbb{P}$-a.s.

$$\mathbb{E}(X_\tau \mid \mathcal{F}_t) \leq X_{t \wedge \tau} \quad (\text{resp. } \geq X_{t \wedge \tau})$$
# Poisson Process

We will now focus on the most basic continuous-time process, called the Poisson process.

This process is an example of a point process that models random arrivals of phenomena in time. For example, the moments of arrival of customers in a queue, or of calls to a central office telephone.

Poisson processes are used for block chain modeling and also for risk modeling.

Let $$(\Omega, \mathcal{F}, \mathbb{P})$$ be a probability space.

A point process on $$\mathbb{R}^{+}$$ is a sequence $$(T_{n})_{n\geq 1}$$ of strictly increasing random variables $$T_{n}$$ with values in $$\mathbb{R}^{+}$$, representing the (random) moments where the studied phenomena/event occur successively (arrivals) : $$T_{n}$$ is the time of arrival of the $$n$$-th event.
# Poisson Process

It is further assumed that $T_n$ converges almost surely towards $+\infty$. By convention, we choose $T_0 = 0$.

$$0 < T_1 < T_2 < \dots < T_n < \dots \to \infty$$

Knowing $(T_n)_{n\geq 1}$ is equivalent to knowing $(\tau_n)_{n\geq 1}$, where $\tau_n = T_n - T_{n-1}$ are the time intervals between the $n-1$-th and the $n$-th events (or arrivals). We have

$$T_n = \sum_{i=1}^{n} \tau_i$$

The condition of strict growth on $(T_n)_{n\geq 1}$ is equivalent to $\tau_n > 0$ for all $n$.
# Definition of Counting Process

The counting process associated with $(T_n)_{n\geq 1}$ is defined by

$$N_t = \sup\{j \geq 1, T_j \leq t\} = \sum_{j \geq 1} 1_{\{T_j \leq t\}}$$

$$\{N_t \geq n\} = \{T_n \leq t\}$$

$$\{N_t \geq n\} = \{\text{at least } n \text{ arrivals before time } t\} = \{T_n \leq t\}$$

If $\omega \in \{T_n \leq t\}$, as the $T_j$ are increasing random variables, $\omega \in \{T_j \leq t\}$, for all $1 \leq j \leq n$ and

$$N_t(\omega) = \sup\{j \geq 1, T_j(\omega) \leq t\} = \sum_{j \geq 1} 1_{\{T_j(\omega) \leq t\}} \geq n$$
# Definition of Counting Process

If $N_t(\omega) = \sum_{j \geq 1} 1_{\{T_j(\omega) \leq t\}} \geq n$, then $\omega \in \{T_j \leq t\}$ for all $1 \leq j \leq n$.

$$N_t = \sup\{j \geq 1, T_j \leq t\} = \sum_{j \geq 1} 1_{\{T_j \leq t\}}$$

$$\{N_t \geq n\} = \{T_n \leq t\}$$

$$\{N_t = n\} = \{T_n \leq t < T_{n+1}\}$$

$$\{N_t = n\} = \{N_t \geq n\} \cap \{N_t \leq n\}$$

$$\{N_t \leq n\} = \{N_t > n\}^c = \{N_t \geq n+1\}^c = \{T_{n+1} \leq t\}^c = \{T_{n+1} > t\}$$

$$\{N_t \geq n\} = \{T_n \leq t\}$$
# Definition of Poisson Process

A Poisson process $N = (N_t)_{t \geq 0}$ with intensity $\lambda > 0$ is a counting process such that :

- $N_0 = 0$.
- The increments are independent : for all $0 \leq t_1 < t_2 < \ldots < t_n$, the random variables $N_{t_1}, N_{t_2} - N_{t_1}, \ldots, N_{t_n} - N_{t_{n-1}}$ are independent.
- The increments are stationary : for all $s, t \geq 0$, $N_{t+s} - N_s$ has the same law as $N_t$.
- For all $t \geq 0$, $N_t$ follows a Poisson law with parameter $\lambda t$ : $N_t \sim \mathcal{P}(\lambda t)$.

The Poisson process is a Lévy process, i.e., with independent and stationary increments.
# Theorem 21

Let $N = (N_t)_{t \geq 0}$ be a Poisson process with intensity $\lambda > 0$. Then :

- The inter-arrival times $\tau_n = T_n - T_{n-1}$ are independent and identically distributed.
- For all $n \geq 1$, $\tau_n$ follows an exponential law with parameter $\lambda : \tau_n \sim \mathcal{E}(\lambda)$.
